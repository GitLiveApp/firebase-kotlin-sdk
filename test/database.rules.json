{
  "rules": {
    ".read": true,
    ".write": true,
    "FirebaseRealtimeDatabaseTest": {
      "lastActivity": {
        ".validate": "!newData.exists() || newData.isNumber()"
      },
      "nested": {
        "lastActivity": {
          ".validate": "!newData.exists() || newData.isNumber()"
        }
      }
    }
  }
}