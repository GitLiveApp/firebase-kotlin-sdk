{
  "rules": {
    ".read": true,
    ".write": true,
    "test": {
      "compatQueries": {
        ".indexOn": ["score", ".value"]
      }
    },
    "FirebaseRealtimeDatabaseTest": {
      "lastActivity": {
        ".validate": "!newData.exists() || newData.isNumber()"
      },
      "nested": {
        "lastActivity": {
          ".validate": "!newData.exists() || newData.isNumber()"
        }
      }
    }
  }
}